# 윈도우 데스크탑에서 코드 작업하기

아이패드에서 만들던 것을 **윈도우 PC에서 이어서 작업**하기 위한 안내입니다.
코딩이 처음이어도 따라 할 수 있도록 하나씩 적었습니다.

**전체 소요 시간: 약 30분** (설치 20분 + 첫 실습 10분)

---

## 준비: 무엇을 왜 설치하나

| 프로그램 | 하는 일 | 비유 |
|---|---|---|
| **GitHub Desktop** | 코드를 내 PC로 가져오고, 고친 것을 다시 올림 | 서류 배달 |
| **VS Code** | 코드를 열어서 보고 고침 | 워드프로세서 |
| (나중에) **Node.js** | 자동 검사(테스트) 실행 | 검사 장비 |

셋 다 무료이고, 설치해도 PC가 느려지지 않습니다.
**Node.js는 지금 안 하셔도 됩니다.** 4단계까지 마친 뒤 필요해지면 그때 하세요.

---

## 1단계 — GitHub Desktop 설치 (약 5분)

1. 브라우저에서 **desktop.github.com** 접속
2. 가운데 **Download for Windows** 버튼 클릭
3. 받은 파일(`GitHubDesktopSetup-x64.exe`)을 **더블클릭**
   - 설치 위치를 묻지 않고 알아서 설치됩니다
4. 설치가 끝나면 자동으로 실행됩니다
5. **Sign in to GitHub.com** 클릭 → 브라우저가 열리면 GitHub 계정으로 로그인
   - 아이패드에서 쓰시던 그 계정(`logos113`)입니다
6. 브라우저에서 **Authorize** 를 누르면 GitHub Desktop으로 돌아옵니다
7. 이름과 이메일을 확인하는 화면이 나오면 그대로 **Finish**

> 로그인이 잘 됐는지 확인: 왼쪽 위에 저장소 목록이 보이면 성공입니다.

---

## 2단계 — 코드를 내 PC로 가져오기 (약 3분)

**Clone**(클론)이라고 부릅니다. 원본은 GitHub에 그대로 두고, 사본을 내 PC에 만드는 것입니다.

1. GitHub Desktop 상단 메뉴 → **File** → **Clone repository**
2. **GitHub.com** 탭에서 목록 중 **logos113/Project-begins** 선택
   - 안 보이면 위 검색창에 `Project-begins` 입력
3. **Local path**(저장 위치)를 확인합니다
   - 기본값 `C:\Users\사용자이름\Documents\GitHub\Project-begins` 로 두시면 됩니다
4. **Clone** 클릭 → 몇 초면 끝납니다

이제 내 PC에 코드가 생겼습니다.

> **중요** — 화면 위쪽 **Current branch** 가 `main` 인지 확인하세요.
> 다른 이름이면 눌러서 `main` 을 선택합니다. 인터넷에 올라가는 것이 이 브랜치입니다.

---

## 3단계 — VS Code 설치하고 코드 열기 (약 7분)

1. 브라우저에서 **code.visualstudio.com** 접속
2. **Download for Windows** 클릭
3. 받은 파일을 더블클릭 → 설치
   - 중간에 체크박스가 여러 개 나오는데, **"Code(으)로 열기" 항목들을 모두 체크**하면 나중에 편합니다
4. 설치 후 **한국어 팩** 설치 (선택)
   - VS Code 왼쪽 세로 막대에서 네모 4개 모양(**확장**) 클릭
   - `Korean` 검색 → **Korean Language Pack** 설치 → 재시작
5. **코드 열기**: GitHub Desktop 으로 돌아가 **Open in Visual Studio Code** 버튼 클릭
   - 안 보이면 상단 **Repository** → **Open in Visual Studio Code**

왼쪽에 파일 목록이 보이면 성공입니다.

```
Project-begins
├── index.html      화면 뼈대
├── style.css       디자인
├── app.js          동작
├── icons/          아이콘 그림
├── tests/          자동 검사
└── tools/          아이콘·스크린샷 만드는 도구
```

---

## 4단계 — 실시간 미리보기 켜기 (약 3분, 강력 추천)

파일을 고칠 때마다 **브라우저가 자동으로 새로고침**되게 하는 기능입니다.
아이패드에서는 못 하던 것이고, 데스크탑 작업의 가장 큰 장점입니다.

1. VS Code 왼쪽 **확장**(네모 4개) 클릭
2. `Live Server` 검색 → **Live Server** (제작자: Ritwick Dey) **설치**
3. 왼쪽 파일 목록에서 **index.html** 을 **마우스 오른쪽 클릭**
4. **Open with Live Server** 선택

브라우저가 열리고 논문 3편이 나타납니다.

> **왜 이게 필요한가요?**
> `index.html` 파일을 그냥 더블클릭해서 열면 브라우저가 PubMed 접속을 막습니다.
> Live Server 는 내 PC 안에 아주 작은 웹서버를 띄워주기 때문에 정상 작동합니다.
> 아이패드에서 GitHub Pages 를 써야 했던 것과 같은 이유입니다.

---

## 5단계 — 직접 고쳐보기 (실습)

전체 흐름을 한 번 익혀봅시다. **색깔 하나만 바꿔보겠습니다.**

**① 파일 열기**

VS Code 왼쪽에서 **style.css** 를 클릭합니다.

**② 색 바꾸기**

위쪽에서 이 줄을 찾습니다 (25번째 줄 근처).

```css
  --accent:    #1c5d7a;   /* 강조색 (차분한 청록) */
```

`#1c5d7a` 를 다른 색으로 바꿉니다. 예를 들어 진한 자주색:

```css
  --accent:    #7a1c3d;   /* 강조색 (차분한 청록) */
```

**③ 저장**

`Ctrl + S` 를 누릅니다.

**④ 확인**

Live Server 로 열어둔 브라우저를 보세요. **저장하는 순간 색이 바뀝니다.**
버튼, 학술지 배지, 링크 색이 모두 함께 바뀝니다.
색 이름표 하나만 고쳐도 전체가 바뀌도록 만들어뒀기 때문입니다.

**⑤ 마음에 들면 GitHub에 올리기**

1. GitHub Desktop 으로 갑니다
2. 왼쪽에 `style.css` 가 뜨고, 오른쪽에 **바뀐 부분이 빨강/초록으로** 표시됩니다
   - 빨강 = 지운 줄, 초록 = 새로 넣은 줄
3. 왼쪽 아래 칸에 무엇을 고쳤는지 적습니다. 예: `강조색을 자주색으로 변경`
4. **Commit to main** 버튼 클릭
5. 위쪽 **Push origin** 버튼 클릭
6. 1~2분 뒤 `https://logos113.github.io/Project-begins/psychiatry/` 를 열면 반영돼 있습니다

**⑥ 되돌리고 싶으면**

색이 마음에 안 들면, Commit 하기 전에는 GitHub Desktop 에서
바뀐 파일을 **오른쪽 클릭 → Discard changes** 하면 원래대로 돌아갑니다.

---

## 6단계 — 매일 지킬 규칙 하나

> **작업 시작 전에 항상 `Fetch origin` → `Pull` 을 먼저 누르세요.**

아이패드에서 고친 것, 또는 제가 고쳐서 올린 것을 먼저 받아와야 합니다.
받아오지 않고 작업하면 나중에 충돌이 생깁니다.

```
① 시작 전   GitHub Desktop → Fetch origin → (Pull 이 뜨면) Pull
② 작업      VS Code 로 고치고 Ctrl+S 저장
③ 끝나면    GitHub Desktop → 설명 적기 → Commit → Push
```

충돌이 나도 **코드는 사라지지 않습니다.** GitHub에 모든 기록이 남아 있습니다. 막히면 물어보세요.

---

## 7단계 — 자동 검사 돌리기 (선택, 나중에)

코드를 고친 뒤 망가진 곳이 없는지 확인하는 장치입니다.
지금은 건너뛰셔도 되고, 코드를 자주 고치게 되면 그때 하세요.

1. **nodejs.org** 접속 → **LTS** 버전 다운로드 → 설치 (기본값 그대로)
2. VS Code 상단 메뉴 → **터미널** → **새 터미널**
3. 아래를 한 줄씩 입력하고 Enter

```bash
npm install jsdom
node tests/logic.test.mjs
node tests/html.test.mjs
node tests/flow.test.mjs
```

전부 `🎉 전체 통과` 가 나오면 정상입니다.

---

## 8단계 — Claude와 함께 작업하기 (선택)

말로 요청해서 코드를 고치는 방식입니다. 세 가지 방법이 있습니다.

**① 브라우저 (설치 불필요, 가장 간단)**

데스크탑 브라우저에서 **claude.ai/code** 접속. 아이패드에서 하던 것과 똑같습니다.

**② VS Code 확장**

VS Code **확장**에서 `Claude Code` 검색 → 설치.
편집기 안에서 바로 대화할 수 있어 파일을 오갈 필요가 없습니다.

**③ 데스크탑 앱**

윈도우용 Claude Code 앱이 있습니다. 설치 방법은 **claude.ai/code** 의 안내를 따르세요.

> 처음에는 **①번**을 쓰시고, 익숙해지면 **②번**으로 옮기시는 것을 권합니다.

---

## 자주 겪는 문제

**GitHub Desktop 에서 Push 가 안 될 때**
→ 먼저 `Fetch origin` 을 누르고, `Pull` 이 뜨면 Pull 한 뒤 다시 Push 하세요.

**Live Server 를 눌러도 브라우저가 안 열릴 때**
→ VS Code 오른쪽 아래 `Port: 5500` 표시를 클릭하거나, 브라우저에서 `http://127.0.0.1:5500` 을 직접 여세요.

**논문이 안 나오고 오류가 뜰 때**
→ 회사·병원 내부망이면 외부 접속이 막혀 있을 수 있습니다. 개인 네트워크에서 시도해 보세요.

**어디를 고쳐야 할지 모르겠을 때**
→ `README.md` 의 "3단계 — 직접 고쳐보기" 를 보세요. 안전하게 바꿔볼 수 있는 것들이 정리돼 있습니다.
